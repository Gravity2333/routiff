{
  "compilerOptions": {
    /* 1. 文件布局 (File Layout) */
    "rootDir": "./src",                         // 源码目录
    "outDir": "./dist",                        // 编译后的输出目录
    "removeComments": true,                    // 移除注释，减小包体积

    /* 2. 环境设置 (Environment Settings) */
    "module": "NodeNext",                      // 现代 Node.js 模块系统
    "moduleResolution": "NodeNext",
    "target": "ESNext",                        // 编译到最新的 JavaScript 语法
    "lib": ["ESNext", "DOM"],                  // 包含最新的 ES 语法和 DOM（如果涉及前端）
    "jsx": "react-jsx",                        // 之前你配置中保留的选项

    /* 3. 产物输出 (Outputs) */
    "sourceMap": true,                         // 方便用户调试你的包
    "declaration": true,                       // 必须开启：生成 .d.ts 类型声明文件
    "declarationMap": true,                    // 生成类型映射文件，方便用户在 IDE 中跳转到源码
    "esModuleInterop": true,                   // 允许导入 CommonJS 模块
    "forceConsistentCasingInFileNames": true,  // 强制文件名大小写一致

    /* 4. 严格类型检查 (Stricter Typechecking) */
    "strict": true,                            // 开启所有严格模式
    "noUncheckedIndexedAccess": true,          // 访问索引时强制检查 undefined
    "exactOptionalPropertyTypes": true,        // 可选属性不能赋值为 undefined (除非明确包含)
    "noImplicitAny": true,
    "strictNullChecks": true,

    /* 5. 推荐配置 (Recommended Options) */
    "skipLibCheck": true,                      // 跳过库文件的类型检查，加快编译速度
    "isolatedModules": true,                   // 确保每个文件都能单独安全转译
    "moduleDetection": "force",
    "noUncheckedSideEffectImports": true       // 防止未使用的副作用导入
  },

  /* 指定包含和排除的范围 */
  "include": [
    "src/**/*"                                 // 只编译 src 目录下的代码
  ],
  "exclude": [
    "node_modules",                            // 排除依赖包
    "dist",                                    // 排除输出目录，防止递归编译
    "**/*.test.ts",                            // 排除测试文件（如果有的话）
    "**/*.spec.ts"
  ]
}